// Stub for running server modules outside Next.js (scripts only).
// In the Next.js runtime the real "server-only" package throws if imported
// from client code; here we just neutralize it.
export {};
